import type { ClientManifest } from '@/runtime/types';

const manifest: ClientManifest = {
  key: 'hitowa',
  name: 'Hitowa',
  enabled: true,

  branding: {
    logo: '/vite.svg',
    favicon: '/vite.svg',
    appName: 'Hitowa',
    primaryColor: '#167133',
  },
};

export default manifest;
